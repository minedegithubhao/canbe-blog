用户信息保护：

- 密钥（公钥和私钥）
- 加盐
- 非对称加密

- JWT令牌

token 和 refresh token的适用场景

token:**发出去之后，在过期前很难主动作废**。

refresh token：退出、改密码、封号、单点登录

access_token: JWT，15分钟左右过期，代码校验签名和exp
refresh_token: 存 Redis，7天/30天过期，用来换新的access_token